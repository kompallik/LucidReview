# ============================================================
# LucidReview Environment Configuration
# Copy to .env and fill in values for local development
# ============================================================

# --- Application ---
NODE_ENV=development
PORT=3000

# --- Database (MySQL) ---
# Dev: MySQL runs in Docker on port 13306 (matches RDS bastion convention)
# Prod: Uses RDS direct connection (no port mapping needed)
DB_HOST=127.0.0.1
DB_PORT=13306
DB_USER=root
DB_PASSWORD=root_dev
DB_NAME=lucidreview
MYSQL_ROOT_PASSWORD=root_dev

# --- FHIR Server ---
HAPI_FHIR_URL=http://localhost:8080/fhir

# --- NLP Service (cTAKES / medspacy) ---
CTAKES_URL=http://localhost:8081

# --- Redis ---
REDIS_URL=redis://localhost:6379

# --- AWS Bedrock ---
# Local dev: use AWS profile credentials from ~/.aws
# Prod (ECS): leave these empty and use the ECS task role (bedrock:invoke)
AWS_REGION=us-east-1
AWS_PROFILE=default
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
BEDROCK_MODEL_ID=us.anthropic.claude-sonnet-4-6

# --- UM System Integration ---
# The existing UM platform REST API base URL
# Leave empty to use mock adapter (for local dev without UM system access)
UM_SYSTEM_BASE_URL=
UM_SYSTEM_API_KEY=

# --- MCP Server ---
# Path to the compiled MCP server index.js
# Default works for Docker (same image). Override for local dev if needed.
MCP_SERVER_PATH=

# --- NLP (cTAKES UMLS - only needed if using real cTAKES, not medspacy) ---
CTAKES_UMLS_USER=
CTAKES_UMLS_PW=
UMLS_API_KEY=

# --- Reviewer UI ---
VITE_API_URL=http://localhost:3000
